@using System.Security.Claims
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] — GestionPublica</title>
    <link rel="stylesheet" href="~/css/admin.css"/>
</head>
<body>
<aside class="sidebar">
    <div class="sidebar-brand">
        <div class="system-name">Gestion Publica</div>
        <div class="role-label">Panel Superadmin</div>
    </div>
    <nav class="sidebar-nav">
        <a href="/Superadmin/Index" class="nav-item @(ViewData["Active"]?.ToString() == "dashboard" ? "active" : "")">
            Dashboard
        </a>
        <a href="/Superadmin/Espacios" class="nav-item @(ViewData["Active"]?.ToString() == "espacios" ? "active" : "")">
            Espacios
        </a>
        <a href="/Superadmin/Penalidades"
           class="nav-item @(ViewData["Active"]?.ToString() == "penalidades" ? "active" : "")">
            Penalidades
        </a>
        <a href="/Superadmin/Usuarios" class="nav-item @(ViewData["Active"]?.ToString() == "usuarios" ? "active" : "")">
            Usuarios
        </a>
    </nav>
    <div class="sidebar-footer">
        <div class="sidebar-user">
            Sesión iniciada como
            <span>@User.Identity.Name @User.FindFirst(ClaimTypes.Surname)?.Value</span>
        </div>
        <form method="post" action="/Auth/Logout">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn-logout">Cerrar sesión</button>
        </form>
    </div>
</aside>
<main class="main-content">
    @RenderBody()
</main>
@RenderSection("Scripts", required: false)
</body>
</html>